// webpack.config.js intentionally removed — kept as a stub to avoid
// interfering with local `expo start` (Metro) while preserving
// the API_URL client changes. If you later need a custom webpack
// configuration for web builds, recreate this file with the
// necessary contents.

module.exports = {};
